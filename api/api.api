syntax = "v1"

import (
	"apis/user.api"
	"apis/tool.api"
)

@server (
	prefix: user
	group:  user
)
service api {
	@handler pinguser
	get /pinguser (PingReq) returns (PingResp)

	@doc "用户注册"
	@handler signup
	post /signup (SignUpReq) returns (SignUpResp)

	@doc "用户登录"
	@handler login
	post /login (LoginReq) returns (LoginResp)
}

@server (
	prefix: tool
	group:  tool
)
service api {
	@doc "申请生成验证码发送到邮箱并存入Redis"
	@handler generateCaptcha
	get /captcha (CaptchaReq) returns (CaptchaResp)
}

